<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Theme.SplashScreen descends from Theme.DeviceDefault.DayNight, so leaving
      windowSplashScreenBackground unset hands the splash colour to the phone's
      dark-mode setting and the splash drawable is never shown on Android 12+.
      Pin it to the brand blue that the icon plate uses.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <!--
          The COMPOSED icon, not the foreground layer. The foreground is now a
          bare white glyph (its card is keyed off for the adaptive layers), so on
          a white splash it would be invisible. ic_launcher carries the blue
          plate and reads cleanly.
        -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>