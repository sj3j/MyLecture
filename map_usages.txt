src/components/AntiCheatDashboard.tsx:70:      const data = snap.docs.map(d => ({ id: d.id, ...d.data() }));
src/components/AntiCheatDashboard.tsx:306:              {logs.map((group: any, idx) => {
src/components/StreakHistoryModal.tsx:48:      const formattedHistory: ActivityDay[] = data.map((docData: any) => {
src/components/StreakHistoryModal.tsx:70:    const rows = history.map(h => `${h.date},${h.wasActive},${h.freezeUsed},${h.timestamp.toISOString()}`).join("\n");
src/components/StreakHistoryModal.tsx:144:                        history.map((h, i) => (
src/components/grades/StudentGradesScreen.tsx:30:      setDegrees(snap.docs.map(d => ({ ...d.data(), id: d.id } as StudentDegree)));
src/components/grades/StudentGradesScreen.tsx:166:          {[1, 2, 3].map(i => (
src/components/grades/StudentGradesScreen.tsx:178:          {Object.entries(groupedDegrees).map(([material, matDegrees]) => {
src/components/grades/StudentGradesScreen.tsx:191:                  {(matDegrees as StudentDegree[]).map(degree => renderDegreeCard(degree))}
src/components/grades/AdminGradesScreen.tsx:93:            {filteredStudents.map(s => {
src/components/grades/AdminGradesScreen.tsx:167:        const st: (UserProfile & { isRegistered?: boolean })[] = usersSnap.docs.map(d => {
src/components/grades/AdminGradesScreen.tsx:215:        setBatches(snap.docs.map(d => ({ ...d.data(), id: d.id })));
src/components/grades/AdminGradesScreen.tsx:279:    setMatchedResults(prev => prev.map(r => {
src/components/grades/AdminGradesScreen.tsx:294:    setMatchedResults(prev => prev.map(r => 
src/components/grades/AdminGradesScreen.tsx:324:         const allStudentIds = students.map(s => s.uid);
src/components/grades/AdminGradesScreen.tsx:364:         const promises = chunk.map((uid: string) => getDoc(doc(db, `degrees/${uid}/exams/${examId}`)));
src/components/grades/AdminGradesScreen.tsx:398:  const selectedStudentIds = new Set(matchedResults.map(r => r.matchedUserId).filter(Boolean)) as Set<string>;
src/components/grades/AdminGradesScreen.tsx:542:                      {CATEGORIES.map(c => (
src/components/grades/AdminGradesScreen.tsx:693:                     {filteredAndSortedResults.map((result) => (
src/components/grades/AdminGradesScreen.tsx:700:                               onChange={(e) => setMatchedResults(prev => prev.map(r => r.rowId === result.rowId ? { ...r, excelName: e.target.value } : r))}
src/components/grades/AdminGradesScreen.tsx:767:            batches.map(batch => (
src/components/BottomNav.tsx:32:        {tabs.map((tab) => {
src/components/GlobalAudioPlayer.tsx:102:               {Array.from({ length: 30 }).map((_, i) => {
src/components/AdminLogsScreen.tsx:59:      ...logs.map(log => {
src/components/AdminLogsScreen.tsx:175:              {filteredLogs.map((log) => (
src/components/ui/Podium.tsx:74:          {[...Array(8)].map((_, i) => (
src/components/ui/Podium.tsx:88:      {podiumOrder.map((student) => {
src/components/SemesterHistoryList.tsx:23:        setHistory(snap.docs.map(doc => ({ id: doc.id, ...doc.data() })));
src/components/SemesterHistoryList.tsx:43:        {history.map(semester => (
src/components/NotificationsModal.tsx:76:        setNotifications(prev => prev.map(n => 
src/components/NotificationsModal.tsx:256:            notifications.map(item => {
src/components/MCQOverlay.tsx:261:               setBankQuestions(prev => prev.map(q => {
src/components/questionBank/BankQuizSetupScreen.tsx:67:             {allTags.map(tag => (
src/components/questionBank/BankQuizScreen.tsx:69:        const next = prev.map(q => q.id === editPromptQuestion.id ? { ...q, ...newQuestionData } : q);
src/components/questionBank/BankQuizScreen.tsx:174:        {questions.map((q, index) => {
src/components/questionBank/BankQuizScreen.tsx:203:                   {q.tags && q.tags.map((tag: string) => (
src/components/questionBank/BankQuizScreen.tsx:228:                {q.choices.map((c, i) => {
src/components/questionBank/AdminQuestionBankScreen.tsx:41:      const alerts = alertsSnap.docs.map(d => d.data());
src/components/questionBank/AdminQuestionBankScreen.tsx:43:      const enrichedQuestions = q.map(question => {
src/components/questionBank/AdminQuestionBankScreen.tsx:53:      setLectures(snap.docs.map(d => ({ id: d.id, ...d.data() })));
src/components/questionBank/AdminQuestionBankScreen.tsx:135:             {tags.map(tag => (
src/components/questionBank/AdminQuestionBankScreen.tsx:170:               {lectures.filter(l => l.category === selectedSubject).map(l => (
src/components/questionBank/AdminQuestionBankScreen.tsx:183:            filtered.map(q => (
src/components/questionBank/AdminQuestionBankScreen.tsx:194:                  {q.tags.map(t => (
src/components/questionBank/UploadPDFModal.tsx:86:          setLectures(snap.docs.map(d => ({ id: d.id, ...d.data() })));
src/components/questionBank/UploadPDFModal.tsx:282:                            {lectures.filter(l => l.category === subjectId).map(l => (
src/components/questionBank/UploadPDFModal.tsx:294:                      {ALL_TAGS.map(tag => (
src/components/questionBank/UploadPDFModal.tsx:307:                      {Array.from(tags).filter((t: string) => !ALL_TAGS.includes(t)).map((tag: string) => (
src/components/questionBank/UploadPDFModal.tsx:429:                  {extractedQuestions.map((q, qIndex) => {
src/components/questionBank/UploadPDFModal.tsx:475:                                    {q.choices.map((choice: any, cIndex: number) => (
src/components/questionBank/AddBankQuestionModal.tsx:92:          setLectures(snap.docs.map(d => ({ id: d.id, ...d.data() })));
src/components/questionBank/AddBankQuestionModal.tsx:219:                    {lectures.filter(l => l.category === subjectId).map(l => (
src/components/questionBank/AddBankQuestionModal.tsx:232:              {ALL_TAGS.map(tag => (
src/components/questionBank/AddBankQuestionModal.tsx:245:              {Array.from(tags).filter((t: string) => !ALL_TAGS.includes(t)).map((tag: string) => (
src/components/questionBank/AddBankQuestionModal.tsx:340:                {choices.map((c, idx) => (
src/components/AudioPlayer.tsx:78:             {Array.from({ length: 40 }).map((_, i) => {
src/components/mcq/MCQReviewScreen.tsx:59:        {filteredQuestions.map((q, index) => {
src/components/mcq/MCQReviewScreen.tsx:93:                  q.choices.map((c: any) => {
src/components/mcq/MCQIntroScreen.tsx:124:              {['وزاري', 'سنين_سابقة', 'سؤال_الدكتور', 'مهم'].map(tag => {
src/components/mcq/MCQQuizScreen.tsx:165:      const updatedQuestions = localQuestions.map(q => q.id === editPromptQuestion.id ? { ...q, ...newQuestionData } : q);
src/components/mcq/MCQQuizScreen.tsx:351:          {localQuestions.map((question, index) => {
src/components/mcq/MCQQuizScreen.tsx:430:                    question.choices.map(c => (
src/components/mcq/MCQQuizScreen.tsx:537:                {questions.map((q, idx) => {
src/components/ChatScreen.tsx:700:          const usersData = snapshot.docs.map(d => ({ id: d.id, ...d.data() }));
src/components/ChatScreen.tsx:722:           const sorted = snap.docs.map(d => d.data()).sort((a: any, b: any) => b.updatedAt - a.updatedAt);
src/components/ChatScreen.tsx:1043:            const users = snap.docs.map((d) => ({
src/components/ChatScreen.tsx:1278:              prev.map((m) =>
src/components/ChatScreen.tsx:1605:      prev.map((m) => {
src/components/ChatScreen.tsx:1639:    return parts.map((part, i) => {
src/components/ChatScreen.tsx:1689:                    prev.map((m) => {
src/components/ChatScreen.tsx:1770:           {isAdminOrModerator && (activeChatFilter === 'all' || activeChatFilter === 'personal') && inboxSessions.map(session => {
src/components/ChatScreen.tsx:1796:           {!isAdminOrModerator && (activeChatFilter === 'all' || activeChatFilter === 'personal') && (settings.privateAccounts || []).map(acc => {
src/components/ChatScreen.tsx:2005:                    {(settings.privateAccounts || []).map((acc, idx) => (
src/components/ChatScreen.tsx:2049:                      {appUsers.map(u => (
src/components/ChatScreen.tsx:2105:          [...messages].reverse().map((msg, idx, arr) => {
src/components/ChatScreen.tsx:2255:                    {mentionFiltered.map((u) => (
src/components/ProfileScreen.tsx:375:                  {['A1', 'A2', 'A3', 'A4', 'B1', 'B2', 'B3', 'B4', 'C1', 'C2', 'C3', 'C4', 'D1', 'D2', 'D3', 'D4'].map(g => (
src/components/AdminUpload.tsx:112:      const newConfigs = validFiles.map((f, i) => {
src/components/AdminUpload.tsx:462:                        {CATEGORIES.filter(c => c.types.includes(type)).map((c) => (
src/components/HomeScreen.tsx:19:    .map((l: Lecture) => ({ 
src/components/HomeScreen.tsx:37:      {downloadedLectures.map(({ lecture }: any) => (
src/components/HomeScreen.tsx:175:        {tabs.map(tab => {
src/components/WeeklyListScreen.tsx:51:      const docs = snapshot.docs.map(doc => ({ id: doc.id, ...doc.data({ serverTimestamps: 'estimate' }) } as Homework));
src/components/WeeklyListScreen.tsx:71:        const lecturesData = lecturesSnapshot.docs.map(doc => ({ id: doc.id, ...doc.data() } as Lecture));
src/components/WeeklyListScreen.tsx:427:                    {CATEGORIES.map(cat => (
src/components/WeeklyListScreen.tsx:489:                    {selectedLectures.map(lec => (
src/components/WeeklyListScreen.tsx:512:                        filteredLectures.map(lec => (
src/components/WeeklyListScreen.tsx:574:            {incompleteHomeworks.map((hw, index) => {
src/components/WeeklyListScreen.tsx:663:                    {hw.lectures.map((lec, i) => {
src/components/WeeklyListScreen.tsx:724:                     {completedHomeworks.map(hw => (
src/components/AdminRecordUpload.tsx:98:      const newConfigs = validFiles.map((f, i) => {
src/components/AdminRecordUpload.tsx:462:                        {CATEGORIES.filter(c => c.types.includes(type)).map((c) => (
src/components/ManageLectureTabsModal.tsx:74:      setTabs(tabs.map(t => t.id === id ? { ...t, name: editName, lectureIds: editLectures } : t));
src/components/ManageLectureTabsModal.tsx:130:              {tabs.map(tab => (
src/components/ManageLectureTabsModal.tsx:174:                            allLectures.filter(l => !searchQuery || l.title.toLowerCase().includes(searchQuery.toLowerCase()) || (t[l.category as keyof typeof t]?.toLowerCase() || '').includes(searchQuery.toLowerCase())).map(lecture => {
src/components/StreakManagement.tsx:59:      const pendingData = pendingSnapshot.docs.map(doc => ({
src/components/StreakManagement.tsx:63:      const validPendingData = pendingData.map((p: any) => {
src/components/StreakManagement.tsx:99:      const studentsData = snapshot.docs.map(doc => {
src/components/StreakManagement.tsx:393:                      {pendingResets.map(pr => (
src/components/StreakManagement.tsx:655:                          {filteredStudents.map(student => (
src/components/RecordsScreen.tsx:78:      const docs = snapshot.docs.map(doc => ({ id: doc.id, ...doc.data({ serverTimestamps: 'estimate' }) } as RecordItem));
src/components/RecordsScreen.tsx:103:    baseRecords = fuse.search(activeSearch).map(result => result.item);
src/components/RecordsScreen.tsx:185:        {CATEGORIES.map(cat => {
src/components/RecordsScreen.tsx:258:            {filteredRecords.map((record, index) => {
src/components/TrueCalendarGrid.tsx:74:              {(isRtl ? dayNamesAr : dayNamesEn).map((day, i) => (
src/components/AnnouncementsScreen.tsx:430:          {Object.entries(groupedPosts).map(([dateKey, postsArray]) => {
src/components/AnnouncementsScreen.tsx:443:                {datePosts.map((post, index) => {
src/components/AnnouncementsScreen.tsx:522:                            {content.split(/(https?:\/\/[^\s]+)/g).map((part, i) => 
src/components/AnnouncementsScreen.tsx:563:                              {post.embeddedLectures.map((lectureId, i) => {
src/components/AnnouncementsScreen.tsx:595:                              {allowedReactions.map(emoji => {
src/components/AnnouncementsScreen.tsx:632:                                  {allowedReactions.map(emoji => (
src/components/AnnouncementsScreen.tsx:796:                      {lectures.map(lecture => (
src/components/SubjectBrowser.tsx:124:          {lectures.map(lecture => (
src/components/SubjectBrowser.tsx:143:        {CATEGORIES.map((cat, index) => {
src/components/SubjectBrowser.tsx:255:        {customTabs.map(tab => (
src/components/SubjectBrowser.tsx:327:              {filteredLectures.map((lecture) => (
src/components/OnboardingSlides.tsx:95:          {slides.map((_, i) => (
src/components/LeaderboardTab.tsx:38:              const userIds = topStudents.map((s: any) => s.userId || s.uid).filter(Boolean);
src/components/LeaderboardTab.tsx:52:                const merged = topStudents.map((s: any) => {
src/components/LeaderboardTab.tsx:74:            const data = snap.docs.map(doc => ({ uid: doc.id, ...doc.data() } as unknown as UserProfile));
src/components/LeaderboardTab.tsx:92:          const rawData = snap.docs.map(doc => doc.data() as UserMCQStats);
src/components/LeaderboardTab.tsx:94:          const userIds = rawData.map(r => r.userId);
src/components/LeaderboardTab.tsx:109:            const merged = rawData.map(stat => ({
src/components/LeaderboardTab.tsx:187:              {streakLeaders.map((leader, index) => {
src/components/LeaderboardTab.tsx:249:            ) : mcqLeaders.slice(3).map((leader, idx) => {
src/components/AdminManagement.tsx:65:      const adminList = snapshot.docs.map(doc => ({
src/components/AdminManagement.tsx:255:                    ].map(perm => (
src/components/AdminManagement.tsx:290:                    {admins.map((admin) => (
src/components/AdminManagement.tsx:308:                              ].map(perm => (
src/components/StudentManagement.tsx:363:      setStudents(students.map(s => (s.baseStudentId || s.id) === targetId ? { ...s, isActive: !s.isActive } : s));
src/components/StudentManagement.tsx:538:        const [csvName, csvEmail, csvPassword, ...rest] = rows[i].split(',').map(s => s?.trim() || '');
src/components/StudentManagement.tsx:609:        cols = cols.map(s => s?.trim() || '');
src/components/StudentManagement.tsx:731:      + neverSignedIn.map(s => `"${s.name}","${s.email}","${s.examCode || ''}","${s.isActive ? (isRtl ? 'مفعل' : 'Active') : (isRtl ? 'معطل' : 'Inactive')}"`).join('\n');
src/components/StudentManagement.tsx:759:  const selectedStudentIds = new Set(matchedExamCodes.map(r => r.matchedStudentId).filter(Boolean));
src/components/StudentManagement.tsx:880:                      {sortedExamCodes.map((result) => (
src/components/StudentManagement.tsx:893:                                 setMatchedExamCodes(prev => prev.map(r => r.rowId === result.rowId ? { 
src/components/StudentManagement.tsx:905:                              {students.filter(s => !selectedStudentIds.has(s.id) || s.id === result.matchedStudentId).map(s => {
src/components/StudentManagement.tsx:1188:                        {filteredStudents.map((student) => (
src/components/StudentManagement.tsx:1349:                {students.filter(s => (s.baseStudentId || s.id) === mergingBaseId).map((acc) => (
